name: My Second Pipeline

on:
  push:
    branches:
      - feature-login

jobs:
  hi:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Custom Script
        run: ./script.sh

