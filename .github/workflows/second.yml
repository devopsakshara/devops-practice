name: My Second Pipeline

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say hello
        run: |
          echo "Hello Akshara 🚀"
          echo "Listing files:"
          ls -lai
on:
  push:
    branches:
      - feature-login

jobs:
  hi:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run Custom Script
        run: ./script.sh

